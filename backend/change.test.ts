import { describe,expect,it } from 'vitest';
import { compareSnapshots, summarizeChanges } from './change';
describe('provider change intelligence',()=>{it('creates no change event for a baseline',()=>expect(compareSnapshots(null,{metrics:{adc:100}})).toEqual([]));it('detects census, legal and competitor changes with severity',()=>{const changes=compareSnapshots({metrics:{adc:100,legalName:'Old LLC',topThreatCcn:'111111'}},{metrics:{adc:120,legalName:'New LLC',topThreatCcn:'222222'}});expect(changes.find(x=>x.key==='adc')?.severity).toBe('high');expect(changes.find(x=>x.key==='legalName')?.severity).toBe('high');expect(changes.find(x=>x.key==='topThreatCcn')?.severity).toBe('high');});it('prioritizes high-severity changes in the alert summary',()=>{const changes=compareSnapshots({metrics:{payment:1000,topThreatCcn:'111111'}},{metrics:{payment:1050,topThreatCcn:'222222'}}),summary=summarizeChanges(changes);expect(summary.severity).toBe('high');expect(summary.summary).toContain('Top collision competitor');});});
