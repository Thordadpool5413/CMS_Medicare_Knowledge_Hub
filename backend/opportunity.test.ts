import { describe,expect,it } from 'vitest';
import { buildCountyOpportunity, buildPhysicianOpportunity, buildSnfOpportunity, reportedSum } from './opportunity';
describe('missing-aware opportunity math',()=>{it('does not invent a SNF score when both census inputs are missing and discounts incomplete ranking evidence',()=>{expect(buildSnfOpportunity(null,null)).toMatchObject({score:null,rankingScore:null,coveragePct:0});expect(buildSnfOpportunity(90,null)).toMatchObject({score:50,rankingScore:34,coveragePct:68});});it('does not let missing physician condition fields outrank equally strong complete zero-condition evidence',()=>{const missing=buildPhysicianOpportunity(800,[null,null,null,null],25),zeros=buildPhysicianOpportunity(800,[0,0,0,0],25);expect(missing.coveragePct).toBe(70);expect(zeros.coveragePct).toBe(100);expect(missing.score).toBeGreaterThan(zeros.score as number);expect(missing.rankingScore).toBeLessThanOrEqual(zeros.rankingScore as number);});it('requires aged Medicare for a ranked county signal while preserving a reported zero',()=>{expect(buildCountyOpportunity(null,100,2,1000,200,4,1).rankingScore).toBeNull();expect(buildCountyOpportunity(0,100,2,1000,200,4,1).rankingScore).not.toBeNull();});it('sums only reported values and counts true zero as reported',()=>{expect(reportedSum([10,null,0,undefined])).toEqual({value:10,reported:2,total:4,coveragePct:50});});});
